# ml-final-project-purchase-intent-prediction
ML final project using the UCI Online Shoppers dataset to predict purchase intent. Includes EDA, feature engineering, and class imbalance handling (over/under-sampling). Trained Logistic Regression, k-NN, and Naive Bayes, evaluated with precision/recall + MAE/RMSE, and added interpretable insights + near-miss analysis.
